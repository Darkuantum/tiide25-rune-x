###############################
# Rune-X Environment Template #
###############################

# Core Database & Auth
# SQLite database file for Prisma.
# For local dev, this should point to a writable path in your project.
DATABASE_URL="file:./prisma/db/dev.db"

# Public URL of your Next.js app.
# In development this is usually http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"

# Secret used by NextAuth.js to sign/encrypt JWTs.
# Generate a strong value for local dev, and an even stronger one for production.
# Example: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"


#####################
# AI / OCR Settings #
#####################

# Optional: Google Gemini API key for OCR, translations, and character meanings.
# Strongly recommended for best accuracy and reliability.
# Get one from: https://aistudio.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=""

# Optional: Hugging Face API token.
# Used for enhanced rate limits and some ancient-Chinese text post-processing.
# Get one from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=""

# Development-only: enable fallback sample OCR data when real OCR fails.
# This is useful for testing the rest of the pipeline without a working OCR setup.
# Set to "true" to enable in development only.
ENABLE_OCR_FALLBACK="false"


######################
# Proxy Configuration #
######################

# Optional SOCKS5 proxy for outbound HTTP(S) requests (Gemini, Hugging Face, etc.).
# Example: socks5://127.0.0.1:1080
SOCKS5_PROXY=""
SOCKS_PROXY=""

# Optional HTTP/HTTPS proxy configuration (alternative to SOCKS5).
# Examples:
# HTTP_PROXY="http://127.0.0.1:8080"
# HTTPS_PROXY="http://127.0.0.1:8080"
HTTP_PROXY=""
HTTPS_PROXY=""
http_proxy=""
https_proxy=""

# Optional: when using proxychains at the system level, these may be set automatically.
# Normally you do NOT need to set these manually in .env.
LD_PRELOAD=""
PROXYCHAINS_CONF_FILE=""


#################
# Misc Settings #
#################

# Node environment; usually "development" for local dev.
# Next.js will also manage NODE_ENV based on the script you run,
# so you can leave this commented out if you prefer.
# NODE_ENV="development"
